version: '3.8'
services:
  flowmate:
    build: .
    ports:
      - "8000:8080"
    environment:
      - PORT=8080
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./flowmate_data:/app/data
